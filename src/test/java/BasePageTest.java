import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;

public class BasePageTest {
    WebDriver  driver;
    BasePageTest(WebDriver driver){
        this.driver = driver;
    }
    public WebElement
}
